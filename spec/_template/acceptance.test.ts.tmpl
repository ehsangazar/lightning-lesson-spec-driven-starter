import { describe, expect, it } from 'vitest'

/**
 * ACCEPTANCE SUITE for SPEC __ID__ · __TITLE__
 *
 * The executable half of SPEC.md. Write these before the implementation exists
 * and watch them fail; a test that has never been red has proved nothing.
 *
 * Rules that keep this suite worth trusting:
 *   - one `describe` per acceptance criterion, labelled A1, A2, …
 *   - assert through the public interface, never on internals
 *   - inject time and randomness; no sleeping, no real timers, no network
 */

describe('A1 · TODO the headline behaviour', () => {
  it('TODO', () => {
    expect.fail('not implemented — write the assertion, then delete this line')
  })
})

describe('A2 · TODO the failure mode you fear', () => {
  it('TODO', () => {
    expect.fail('not implemented — write the assertion, then delete this line')
  })
})

describe('A3 · TODO the boundary a plausible version gets wrong', () => {
  it('TODO', () => {
    expect.fail('not implemented — write the assertion, then delete this line')
  })
})
